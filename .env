# Environment variables for this project.
# Loaded automatically by uv when UV_ENV_FILE=.env is set (e.g. via activate.sh).
# Run `source activate.sh` once in a shell to enable all settings below.

# Redirect __pycache__ to .cache/pycache so the source tree stays clean.
PYTHONPYCACHEPREFIX=.cache/pycache
