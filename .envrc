source_up_if_exists

# source the main activation script
source "./activate.sh"

# source user-specific environment variables if present
if [ -f "$WORKSPACE_ROOT/.envrc.user" ]; then
    source "$WORKSPACE_ROOT/.envrc.user"
fi
